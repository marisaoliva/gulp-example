{
  "name": "gulp-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "stylus": "gulp stylus",
    "dust":"gulp dust",
    "start": "gulp stylus && gulp dust && gulp watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gulp-dust": "^3.0.0"
  }
}
